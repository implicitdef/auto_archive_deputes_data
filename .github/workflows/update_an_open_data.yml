on:
  workflow_dispatch:
  schedule:
    - cron: '0 1 * * *' # every day at 1h

jobs:
  main-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
      - uses: actions/setup-node@v3
        with:
          node-version: '16'
          cache: 'yarn'
      - run: node --version
      - run: yarn --version
      - run: yarn install --frozen-lockfile
      - run: yarn start update_an_open_data
      - uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: Auto commit of AN open data
